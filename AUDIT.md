# تقرير فحص وإصلاح Hotspot Tracker — 14 سبتمبر 2026

المراجعة بدأت من `36609942f2ebd763c69c5413845edbea0bb0af0f` على `main`.
شملت ملفات المشروع الأصلية الـ44: واجهة HTML وسلسلتي تحميلها، ملفات API، محركات التحليل، مخطط سجل الإشارات، إعدادات التشغيل والاختبارات. شمل الفحص قراءة الكود، تحليل JavaScript ساكنًا، وإعادة إنتاج أخطاء محددة باختبارات معزولة. لا تعني هذه المراجعة إثبات خلو التطبيق من كل عيب محتمل.

## ما اكتُشف وما صُحّح

| المجال | المشكلة المؤكدة | الإصلاح ومصدره |
|---|---|---|
| حماية المزامنة | طلب POST عام إلى sync كان يضيف سر cron داخليًا ويشغّل العمل المحمي | التحقق من Bearer قبل أي قراءة أو كتابة في [api/sync.js](api/sync.js) |
| حماية العامل | غياب WORKER_SECRET كان يلغي التحقق؛ وقبول السر في عنوان الطلب يعرّضه للسجلات | رفض التشغيل عند غياب السر، وقبوله في الترويسة فقط في [api/worker.js](api/worker.js)؛ حماية أوضاع التشغيل المعدّلة للبيانات في [api/group-six.js](api/group-six.js) |
| رقم السحب الرسمي | استرجاع السحب التاريخي التالي كان يخفض الرقم الرسمي المعروف، ثم قد يعلن اكتمال المزامنة | الاحتفاظ بأحدث رقم رسمي في [api/sync.js](api/sync.js) |
| حالة المزامنة | انتهاء دفعة الـ25 سحبًا اليدوية لا يعني اكتمال التتبع؛ الواجهة كانت تعرض «محدّث» عند synced=false | حساب التأخر اليدوي المتبقي واستخدام caughtUp والتأخر الفعلي في [api/sync.js](api/sync.js) و[index-base.html](index-base.html) |
| السحوبات المفقودة | عدة مسارات كانت تحرّك المؤشر إلى آخر صف متاح حتى مع وجود فجوة قبله | معالجة السلسلة المتصلة الصحيحة فقط في [lib/draw-sequence.js](lib/draw-sequence.js)، العامل، Group Five/Six والتتبع الأولي Advanced |
| الطلبات المتزامنة | طلب أبطأ يستطيع إعادة مؤشر مجموعة إلى الوراء أو تحديث دورة أُعيد ضبطها | تحديث مشروط برقم بداية الدورة وبأن المؤشر الحالي أصغر في [lib/tracking-cursor.js](lib/tracking-cursor.js). هذا ليس قفلًا أو معاملة كاملة لعمليات إنشاء/حذف المجموعات |
| حد التتبع | العامل العام يطبّق 120 سحبًا على مجموعات Five/Six ذات نافذة 20 سحبًا | توحيد حدود 20/120 في العامل والإغلاق؛ منع الدوران قبل اكتمال معالجة النافذة |
| عبور منتصف الليل | معالجة نهاية اليوم ترفض سحوبات المساء لأن الساعة أكبر من 02:00، رغم وقوعها قبل سحب الإغلاق | الاعتماد على نطاق أرقام السحوبات المنتهي بسحب الإغلاق الموثق في [api/cron.js](api/cron.js) |
| السياق الحي | حد 220 صفًا مرتبة تصاعديًا يخفي نهاية دورة اليوم | رفع حد القراءة إلى 500 في [api/health.js](api/health.js) |
| نافذة التوقع | عند الحد الأعلى لفجوة تاريخية، تُنشأ نافذة معكوسة أو نقطة توقع خارج النطاق | تصحيح حدود الانتقال بين SHORT/LONG/OUTSIDE_HISTORY في [api/health.js](api/health.js) |
| بطاقة Best | تعرض آخر نتيجة قوية بدل أعلى نتيجة؛ Fusion لا يوفّر أفضل نتيجة تاريخية | قراءة أفضل نتيجة مستقلة وعرض وقتها، مع إبقاء آخر ظهور منفصلًا في [api/fusion.js](api/fusion.js)، [api/group.js](api/group.js)، والواجهة |
| عدادات النتائج | العد من آخر 100 نتيجة فقط، وطلب limit=5000 لا يتجاوز وحده سقف Supabase | تلخيص نتائج الدورة على صفحات في [lib/db-pages.js](lib/db-pages.js) و[lib/manual-summary.js](lib/manual-summary.js)؛ إظهار ≥ عند بلوغ حد الملخص 5000 |
| حقول الإدخال | التحديث الدوري يستبدل حقولًا أخرى أثناء تحرير المجموعة | إبقاء مدخلات المجموعة أثناء التركيز على أحد حقولها في [index-base.html](index-base.html) |
| تكرار الإشارة | استمرار STRONG بعد حسم نتيجتها يفتح حلقات إضافية لنفس التشغيل المستمر | تمديد أحدث حلقة حتى لو حُسمت نتيجتها، دون تحريك نافذتها، وتجاهل الملاحظات الأقدم في [lib/signal-ledger.js](lib/signal-ledger.js) |
| حسم النتائج | عدد الصفوف وحده لا يضمن اكتمال النافذة؛ ترتيب الصفوف قد يغيّر أول إصابة | التحقق من الأرقام المتصلة ومن 20 رقمًا صالحًا لكل سحب، ورفض التكرار/النقص وترتيب النتائج |
| مقارنة الاحتمالات | تغيير window أو threshold يغيّر احتمال الصدفة بينما يبقي نجاحات مسجلة بتعريف آخر | تصفية الحلقات بحسب النافذة وإعادة حساب النجاح من best_hit_count بحسب العتبة؛ عدم عرض زمن إصابة العتبة 4 لعتبة أخرى |
| الحساب العددي | ذيل التوزيع ذو الاحتمال الصغير يفقد الدقة بسبب الطرح من 1؛ p=0/1 يعطي نتائج غير صالحة؛ تخطيط قوة مستحيلة يدخل بحثًا طويلًا | جمع الذيل الأصغر مباشرة، معالجة الحدود، بحث ثنائي للحد الحرج ورفض البدائل المستحيلة |
| تشغيل السجل | السجل موجود لكن cron لا يسجّل فيه | إضافة التسجيل ثم الحسم، خلف SIGNAL_LEDGER_ENABLED بعد تجهيز الجدول؛ انظر [WIRING.md](WIRING.md) |
| الدراسات الخلفية | بعض نوافذ المقارنة تستخدم نتائج بعد نهاية التحقق؛ الإشارة المستمرة عند فصل العينة تُحسب كبداية جديدة | حصر النتيجة داخل القطع الزمني، واستعادة حالة الإشارة السابقة في [api/five-study.js](api/five-study.js) و[api/five-cross-test.js](api/five-cross-test.js) |
| صدق نتائج الاختبار الخلفي | تخطي دورة فاشلة مع إبقاء المقام ثابتًا في مقارنة Group Five | إيقاف المقارنة بخطأ واضح عند دورة ناقصة في [api/backtest-group-five.js](api/backtest-group-five.js) |
| الأرشيف | عمر أقدم صف بالنسبة إلى اليوم يُعرض كتغطية بيانات؛ أقدم صف جديد قد يكون فشل حفظه | حساب المدة بين أقدم وأحدث سحب محفوظ، والإبلاغ عن الصفوف التي حُفظت فعلًا |
| التحقق من المدخلات | قبول أرقام كسرية، إسقاط مدخلات زائدة/مكررة بصمت، وأخطاء مستخدم تُعاد كـ500 | تشديد التحقق، إعادة 400 للمدخلات غير الصحيحة و405 للطرق غير المدعومة، وإبقاء Bulls Eye المفقود null |
| اتصالات الشبكة | طلبات المصدر الرسمي وقاعدة البيانات دون مهلة | إضافة مهلة 10 ثوانٍ للمصدر و15 ثانية لطلب قاعدة البيانات في [api/lib.js](api/lib.js) |
| ملف الحالة القديم | state.js في الجذر يستورد مسارًا غير موجود ويكرر تنفيذًا قديمًا | تحويله إلى إحالة للتنفيذ المصان [api/state.js](api/state.js) |
| رصد فشل التشغيل | أخطاء المراحل الداخلية تُدفن داخل استجابة ناجحة؛ workflow يتحقق من HTTP فقط | إرجاع partialFailures وحالة فشل، وفحص JSON في workflow؛ تمرير السر كمتغير بيئة بدل إدراجه داخل نص shell |
| الاختبارات والتثبيت | اختبارات المقارنة تطبع الاختلاف ولا تفشل؛ لا أمر اختبار موحّد أو lockfile | assertions فعلية، اختبارات إعادة إنتاج، اختبار DOM للواجهة، npm test، package-lock وCI |

## التحقق المنفذ

- `npm test` نجح: **42 اختبارًا** للخدمات، السجل، الفجوات، الحدود والواجهة الفعلية باستخدام jsdom.
- مقارنة المحرك المحسّن بالمرجع: **6/6 نتائج متطابقة**. لم يتغير كود محرك precursor ولا مرجعه.
- محاكاة السجل: **1200 حلقة، 64 نجاحًا مقابل 74.5 متوقعًا بالصدفة**؛ أعاد التقرير نتيجة عدم وجود دليل على تفوق في هذه العينة.
- اختبارات الأثر المزروع 1.5x و2x و3x نجحت في العينات المحددة بالبذور، مع assertions.
- فحص تركيب JavaScript وطبقتي توليد HTML، وتشغيل الصفحة بمحاكاة استجابات API، نجح. فحص القواعد الساكنة لم يجد أخطاء غير معرّفة في ملفات التطبيق؛ إشارة loadAll عند فحص السكربت الثاني وحده حُلّت بتشغيل السكربتين معًا، فهي دالة عامة يعرّفها الأول.
- `git diff --check` نجح.
- فحص الاعتماديات الإنتاجية عبر `npm audit --omit=dev`: صفر ثغرات معروفة بحسب نتيجة سجل npm وقت الفحص.

مصادر التحقق القابلة للإعادة: [verify/audit.test.js](verify/audit.test.js)، [verify/browser.test.js](verify/browser.test.js)، [verify/verify-identical.js](verify/verify-identical.js)، و[سير CI](.github/workflows/verify.yml).

## حدود التحقق ومتطلبات التشغيل

1. لم تُستخدم بيانات اعتماد Supabase أو تُنفذ تغييرات على قاعدة الإنتاج، ولم يُنشر هذا الفرع إلى الإنتاج. الاختبارات تستخدم قاعدة وهمية واستجابات شبكة محاكية؛ لا تثبت صحة مخطط الإنتاج أو سياسات RLS أو استمرارية الصفحة الرسمية.
2. لتفعيل سجل النتائج: تطبيق `ledger-schema.sql` ثم ضبط `SIGNAL_LEDGER_ENABLED=true`. يبقى معطلًا افتراضيًا لحماية التشغيل القائم إذا لم يكن الجدول موجودًا. لا يصح اختلاق حلقات قديمة لم تُسجّل قبل النتيجة.
3. مسارات إدارة المجموعات اليدوية ما زالت مبنية على نموذج تطبيق مشترك دون هوية مستخدم. إصلاح تجاوز حماية cron لا يضيف نظام حسابات أو عزل بيانات. يلزم اختيار سياسة وصول قبل اعتباره تطبيقًا خاصًا متعدد المستخدمين.
4. حماية المؤشر تقلل سباقات التحديث، لكنها لا تجعل عمليات إعادة ضبط/إنشاء/حذف مجموعات متعددة متزامنة معاملة ذرية. معالجة ذلك بالكامل تتطلب مخطط قاعدة البيانات الحي وقيوده ومعاملات أو دوال قاعدة بيانات.
5. سجل الإشارات يحلل الأهداف الخماسية التي يصلها cron ضمن حده الحالي، وهو 6 مجموعات STRONG_MANUAL نشطة. احتمالاته وفواصل الثقة تفترض استقلال الحلقات؛ التداخل بين النوافذ أو الأهداف والمقارنات المتعددة يجعل النتائج استكشافية، وليس وعدًا بالتنبؤ أو الربح.
6. عدادات الملخص محدودة بـ5000 نتيجة مع علامة حد صريحة؛ حجم الصفحة 1000 مبني على إعداد Supabase الافتراضي. يجب مواءمته إذا خُفّض سقف الاستجابة في المشروع.
7. تغطية الأرشيف هنا مدة بين الطرفين، وليست شهادة بأن كل سحب بينهما موجود. تُمنع نتائج التتبع وحسم السجل من القفز فوق الفجوات، لكن محركات الدراسات الأقدم لا تُعاد كتابتها بهذا الإصلاح.

## مراجع خارجية

- [Vercel: تأمين Cron بترويسة Authorization وCRON_SECRET](https://vercel.com/docs/cron-jobs/manage-cron-jobs).
- [Supabase: سقف الصفوف الافتراضي وضرورة تقسيم القراءة](https://supabase.com/docs/reference/javascript/select).

المرجع الأساسي لكل اكتشاف هو ملفات المستودع واختبارات إعادة الإنتاج المرتبطة أعلاه.
