{
  "name": "hotspot-tracker",
  "version": "3.0.0",
  "private": true,
  "description": "Statistical tracking dashboard for California Lottery Hot Spot draws, measured against the exact chance model.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "simulate": "node test/simulate.js 40",
    "check": "npm run test && npm run simulate"
  },
  "dependencies": {}
}
